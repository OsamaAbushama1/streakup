# ==================== SERVER CONFIGURATION ====================
NODE_ENV=development
PORT=5000

# ==================== DATABASE ====================
# MongoDB connection string
# Local: mongodb://localhost:27017/streakup
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/streakup
MONGO_URI=mongodb://localhost:27017/streakup

# ==================== JWT AUTHENTICATION ====================
# CRITICAL: Generate a strong secret using: openssl rand -base64 64
# This MUST be changed in production! Never use default values!
# Minimum 32 characters, recommended 64+ characters for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-openssl-rand-base64-64

# ==================== FRONTEND & ADMIN URLS ====================
# Used for CORS configuration
# Development:
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3000/admin

# Production (Render):
# FRONTEND_URL=https://your-frontend-name.onrender.com
# ADMIN_URL=https://your-frontend-name.onrender.com/admin

# ==================== EMAIL CONFIGURATION ====================
# For password reset emails and notifications
# Gmail: Use App Password (not your regular password)
# https://support.google.com/accounts/answer/185833
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password

# ==================== CLOUDINARY CONFIGURATION ====================
# For image uploads and storage (profile pictures, challenge images)
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ==================== STRIPE CONFIGURATION ====================
# For payment processing (certificates, rewards)
# Get keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# ==================== OPTIONAL CONFIGURATION ====================
# Cookie domain for production (leave empty for localhost)
# Only needed if you have a custom domain
# COOKIE_DOMAIN=.yourdomain.com

# ==================== RENDER DEPLOYMENT NOTES ====================
# When deploying to Render:
# 1. Generate strong JWT_SECRET: openssl rand -base64 64
# 2. Set all variables in Render Dashboard → Environment
# 3. Update FRONTEND_URL to your Render frontend URL
# 4. Use MongoDB Atlas connection string (not localhost)
# 5. Ensure NODE_ENV=production
# 6. Never commit your real .env file to Git!

# ==================== SECURITY CHECKLIST ====================
# Before deploying to production:
# [ ] JWT_SECRET is at least 64 characters (use openssl rand -base64 64)
# [ ] MONGO_URI points to MongoDB Atlas (not localhost)
# [ ] EMAIL credentials are set (for password reset)
# [ ] CLOUDINARY credentials are set (for image uploads)
# [ ] FRONTEND_URL matches your deployed frontend
# [ ] NODE_ENV is set to "production"
# [ ] All placeholder values are replaced with real values
